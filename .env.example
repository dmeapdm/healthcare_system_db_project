# Copiar este archivo a ".env" y completar con los valores reales.
# NUNCA subir el ".env" real a un repositorio.

DB_HOST=localhost
DB_PORT=3306
DB_USER=tecnico_biomedica
DB_PASSWORD=biomedica123
DB_NAME=healthcare_system_db

# Generar una clave larga y aleatoria, ej: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CAMBIAR_EN_PRODUCCION_por_una_clave_larga_y_aleatoria
ACCESS_TOKEN_EXPIRE_MINUTES=60
